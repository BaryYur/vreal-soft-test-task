{
  "name": "vreal-test-task",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for VReal soft test task app (NestJS + NextJS)",
  "author": "BaryYur",
  "type": "commonjs",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run start:dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "concurrently \"npm run start -w backend\" \"npm run start -w frontend\"",
    "format": "npm run format -w backend && npm run format -w frontend",
    "postinstall": "npm run postinstall -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
